spring:
  application:
    name: registry

info:
  build:
    name: Registry Service
    description: ReportPortal Registry and Configuration Service
    version: '@version@'
    branch: '@branch@'
    repo: '@repo@'